import React from 'react';

import { Box, Header, Description } from './styled';

const About = () => (
  <Box>
    <Header>About</Header>
    <Description>Notes application where users can create new notes, delete old notes, change the order of notes using Drag&Drop, sort notes by title and date of creation, and also share notes with other users.</Description>
  </Box>
);

export default About;
